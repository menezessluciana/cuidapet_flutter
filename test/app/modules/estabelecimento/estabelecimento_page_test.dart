import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuidapet_curso/app/modules/estabelecimento/estabelecimento_page.dart';

void main() {
  testWidgets('EstabelecimentoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EstabelecimentoPage(title: 'Estabelecimento')));
    //  final titleFinder = find.text('Estabelecimento');
    //  expect(titleFinder, findsOneWidget);
  });
}
